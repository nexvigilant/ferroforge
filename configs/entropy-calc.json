{
  "domain": "entropy-calc.nexvigilant.com",
  "url_pattern": "/*",
  "title": "Entropy Calculator",
  "description": "Information entropy computation, Shannon entropy, and disorder measurement",
  "tools": [
    {
      "name": "entropy-compute",
      "description": "normalized or conditional entropy from probability distributions or counts.",
      "parameters": [],
      "outputSchema": {
        "type": "object",
        "properties": {
          "status": {
            "type": "string",
            "description": "ok | error"
          }
        },
        "required": [
          "status"
        ]
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false
      }
    }
  ],
  "proxy": "rust-native"
}
