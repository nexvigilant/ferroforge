{
  "domain": "qbr.nexvigilant.com",
  "url_pattern": "/*",
  "title": "Quantitative Benefit-Risk",
  "description": "QBR scoring, benefit-risk computation, and therapeutic window analysis",
  "tools": [
    {
      "name": "qbr-compute",
      "description": "Compute full QBR with all available forms (simple, Bayesian, composite, therapeutic window).",
      "parameters": [],
      "outputSchema": {
        "type": "object",
        "properties": {
          "status": {
            "type": "string",
            "description": "ok | error"
          }
        },
        "required": [
          "status"
        ]
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false
      }
    },
    {
      "name": "qbr-simple",
      "description": "Compute simple benefit-risk ratio from one benefit and one risk contingency table.",
      "parameters": [],
      "outputSchema": {
        "type": "object",
        "properties": {
          "status": {
            "type": "string",
            "description": "ok | error"
          }
        },
        "required": [
          "status"
        ]
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false
      }
    },
    {
      "name": "qbr-therapeutic-window",
      "description": "Compute therapeutic window from efficacy and toxicity Hill curve parameters.",
      "parameters": [],
      "outputSchema": {
        "type": "object",
        "properties": {
          "status": {
            "type": "string",
            "description": "ok | error"
          }
        },
        "required": [
          "status"
        ]
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false
      }
    }
  ],
  "proxy": "rust-native"
}
