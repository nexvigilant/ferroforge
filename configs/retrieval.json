{
  "domain": "retrieval.nexvigilant.com",
  "url_pattern": "/*",
  "title": "Retrieval Engine",
  "description": "Information retrieval, search operations, and knowledge recall",
  "tools": [
    {
      "name": "retrieval-ingest",
      "description": "Deduplicates by source_id (updates existing entries).",
      "parameters": [],
      "outputSchema": {
        "type": "object",
        "properties": {
          "status": {
            "type": "string",
            "description": "ok | error"
          }
        },
        "required": [
          "status"
        ]
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false
      }
    },
    {
      "name": "retrieval-query",
      "description": "Recent queries are cached with configurable TTL.",
      "parameters": [],
      "outputSchema": {
        "type": "object",
        "properties": {
          "status": {
            "type": "string",
            "description": "ok | error"
          }
        },
        "required": [
          "status"
        ]
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false
      }
    },
    {
      "name": "retrieval-stats",
      "description": "`retrieval_stats` \u2014 Pipeline health, cache metrics, and query analytics.",
      "parameters": [],
      "outputSchema": {
        "type": "object",
        "properties": {
          "status": {
            "type": "string",
            "description": "ok | error"
          }
        },
        "required": [
          "status"
        ]
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false
      }
    }
  ],
  "proxy": "rust-native"
}
