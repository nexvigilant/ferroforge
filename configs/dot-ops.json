{
  "domain": "dot-ops.nexvigilant.com",
  "url_pattern": "/*",
  "title": "Dot Operations",
  "description": "Highway dispatch manifests and highway verification operations",
  "tools": [
    {
      "name": "dot-dispatch-manifest",
      "description": "Dispatch a transit manifest across domains",
      "parameters": [],
      "outputSchema": {
        "type": "object",
        "properties": {
          "status": {
            "type": "string",
            "description": "ok | error"
          }
        },
        "required": [
          "status"
        ]
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false
      }
    },
    {
      "name": "dot-verify-highway",
      "description": "Verify highway safety (CAP-019). NHTSA-style route integrity check.",
      "parameters": [],
      "outputSchema": {
        "type": "object",
        "properties": {
          "status": {
            "type": "string",
            "description": "ok | error"
          }
        },
        "required": [
          "status"
        ]
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false
      }
    }
  ],
  "proxy": "rust-native"
}
