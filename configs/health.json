{
  "domain": "health.nexvigilant.com",
  "url_pattern": "/*",
  "title": "Health Assessment",
  "description": "Impact measurement, signal validation, and health scoring",
  "tools": [
    {
      "name": "health-measure-impact",
      "description": "Measure public health impact of signals",
      "parameters": [],
      "outputSchema": {
        "type": "object",
        "properties": {
          "status": {
            "type": "string",
            "description": "ok | error"
          }
        },
        "required": [
          "status"
        ]
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false
      }
    },
    {
      "name": "health-validate-signal",
      "description": "Validate signal efficacy against ground truth (FDA-style validation)",
      "parameters": [],
      "outputSchema": {
        "type": "object",
        "properties": {
          "status": {
            "type": "string",
            "description": "ok | error"
          }
        },
        "required": [
          "status"
        ]
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false
      }
    }
  ],
  "proxy": "rust-native"
}
