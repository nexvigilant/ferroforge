{
  "domain": "visual.nexvigilant.com",
  "url_pattern": "/*",
  "title": "Visual Intelligence",
  "description": "Visual rendering, image analysis, and graphical output generation",
  "tools": [
    {
      "name": "visual-color-analyze",
      "description": "/// Returns RGBA quantities, luminance, named match, and grounding.",
      "parameters": [],
      "outputSchema": {
        "type": "object",
        "properties": {
          "status": {
            "type": "string",
            "description": "ok | error"
          }
        },
        "required": [
          "status"
        ]
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false
      }
    },
    {
      "name": "visual-shape-classify",
      "description": "Prima representation.",
      "parameters": [],
      "outputSchema": {
        "type": "object",
        "properties": {
          "status": {
            "type": "string",
            "description": "ok | error"
          }
        },
        "required": [
          "status"
        ]
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false
      }
    },
    {
      "name": "visual-shape-list",
      "description": "/// Returns the catalog of visual primitives with their T1 groundings.",
      "parameters": [],
      "outputSchema": {
        "type": "object",
        "properties": {
          "status": {
            "type": "string",
            "description": "ok | error"
          }
        },
        "required": [
          "status"
        ]
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false
      }
    }
  ],
  "proxy": "rust-native"
}
