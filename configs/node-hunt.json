{
  "domain": "node-hunt.nexvigilant.com",
  "url_pattern": "/*",
  "title": "Node Hunter",
  "description": "Isolated node detection, dependency scanning, and orphan identification",
  "tools": [
    {
      "name": "node-hunt-isolate",
      "description": "Isolate a specific node from the network",
      "parameters": [],
      "outputSchema": {
        "type": "object",
        "properties": {
          "status": {
            "type": "string",
            "description": "ok | error"
          }
        },
        "required": [
          "status"
        ]
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false
      }
    },
    {
      "name": "node-hunt-scan",
      "description": "Returns per-boundary classification, distance, and overall regime.",
      "parameters": [],
      "outputSchema": {
        "type": "object",
        "properties": {
          "status": {
            "type": "string",
            "description": "ok | error"
          }
        },
        "required": [
          "status"
        ]
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false
      }
    }
  ],
  "proxy": "rust-native"
}
