{
  "domain": "statemind.nexvigilant.com",
  "url_pattern": "/*",
  "title": "Statemind Engine",
  "description": "State machine reasoning, mind-state tracking, and cognitive state management",
  "tools": [
    {
      "name": "statemind-analyze-word",
      "description": "Analyze a word through the full DNA pipeline.",
      "parameters": [],
      "outputSchema": {
        "type": "object",
        "properties": {
          "status": {
            "type": "string",
            "description": "ok | error"
          }
        },
        "required": [
          "status"
        ]
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false
      }
    },
    {
      "name": "statemind-constellation",
      "description": "Analyze a constellation of words (pairwise resonance, mutation, clustering).",
      "parameters": [],
      "outputSchema": {
        "type": "object",
        "properties": {
          "status": {
            "type": "string",
            "description": "ok | error"
          }
        },
        "required": [
          "status"
        ]
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false
      }
    }
  ],
  "proxy": "rust-native"
}
