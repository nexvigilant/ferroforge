{
  "domain": "test-history.nexvigilant.com",
  "url_pattern": "/*",
  "title": "Test History",
  "description": "Flaky test detection, test result querying, and historical test analysis",
  "tools": [
    {
      "name": "test-history-flaky",
      "description": "Identify flaky tests that appear in fail_names across multiple runs.",
      "parameters": [],
      "outputSchema": {
        "type": "object",
        "properties": {
          "status": {
            "type": "string",
            "description": "ok | error"
          }
        },
        "required": [
          "status"
        ]
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false
      }
    },
    {
      "name": "test-history-query",
      "description": "Query test run history with optional filters.",
      "parameters": [],
      "outputSchema": {
        "type": "object",
        "properties": {
          "status": {
            "type": "string",
            "description": "ok | error"
          }
        },
        "required": [
          "status"
        ]
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false
      }
    }
  ],
  "proxy": "rust-native"
}
