{
  "domain": "www.linkedin.com",
  "private": true,
  "url_pattern": "/*",
  "title": "LinkedIn Professional Network",
  "description": "Read posts, track analytics, and search LinkedIn content — read-only PV knowledge sharing intelligence",
  "tools": [
    {
      "name": "get-profile",
      "description": "Get your LinkedIn profile information (name, headline, email, picture)",
      "parameters": [],
      "annotations": { "readOnlyHint": true, "destructiveHint": false },
      "outputSchema": {
        "type": "object",
        "properties": {
          "status": { "type": "string", "description": "ok | error" },
          "name": { "type": "string" },
          "given_name": { "type": "string" },
          "family_name": { "type": "string" },
          "headline": { "type": "string" },
          "email": { "type": "string" },
          "picture": { "type": "string" },
          "locale": { "type": "string" },
          "profile_url": { "type": "string" }
        },
        "required": ["status"]
      }
    },
    {
      "name": "get-my-posts",
      "description": "List your recent LinkedIn posts with text preview and engagement counts (requires Community Management API)",
      "parameters": [
        {
          "name": "limit",
          "type": "integer",
          "description": "Maximum posts to return (default 10, max 50)",
          "required": false
        }
      ],
      "annotations": { "readOnlyHint": true, "destructiveHint": false },
      "outputSchema": {
        "type": "object",
        "properties": {
          "status": { "type": "string", "description": "ok | error | pending_approval" },
          "count": { "type": "integer" },
          "posts": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "post_urn": { "type": "string" },
                "text": { "type": "string" },
                "created_at": { "type": "string" },
                "visibility": { "type": "string" }
              }
            }
          }
        },
        "required": ["status"]
      }
    },
    {
      "name": "get-post",
      "description": "Get full text and metadata of a specific LinkedIn post by URN or URL (requires Community Management API)",
      "parameters": [
        {
          "name": "post_id",
          "type": "string",
          "description": "Post URN (urn:li:share:..., urn:li:activity:...) or LinkedIn post URL",
          "required": true
        }
      ],
      "annotations": { "readOnlyHint": true, "destructiveHint": false },
      "outputSchema": {
        "type": "object",
        "properties": {
          "status": { "type": "string", "description": "ok | error" },
          "post_urn": { "type": "string" },
          "author": { "type": "string" },
          "text": { "type": "string" },
          "created_at": { "type": "string" },
          "visibility": { "type": "string" },
          "media": { "type": "array", "items": { "type": "object" } }
        },
        "required": ["status"]
      }
    },
    {
      "name": "get-post-analytics",
      "description": "Get engagement metrics (likes, comments, shares) for a post (impressions require Marketing API)",
      "parameters": [
        {
          "name": "post_id",
          "type": "string",
          "description": "Post URN or LinkedIn post URL",
          "required": true
        }
      ],
      "annotations": { "readOnlyHint": true, "destructiveHint": false },
      "outputSchema": {
        "type": "object",
        "properties": {
          "status": { "type": "string", "description": "ok | error" },
          "post_urn": { "type": "string" },
          "impressions": { "type": "integer" },
          "likes": { "type": "integer" },
          "comments": { "type": "integer" },
          "shares": { "type": "integer" },
          "clicks": { "type": "integer" },
          "engagement_rate": { "type": "number", "description": "Engagement % (interactions / impressions * 100)" }
        },
        "required": ["status"]
      }
    },
    {
      "name": "get-comments",
      "description": "Get comments on a specific LinkedIn post (requires Community Management API)",
      "parameters": [
        {
          "name": "post_id",
          "type": "string",
          "description": "Post URN or LinkedIn post URL",
          "required": true
        },
        {
          "name": "limit",
          "type": "integer",
          "description": "Maximum comments to return (default 20, max 100)",
          "required": false
        }
      ],
      "annotations": { "readOnlyHint": true, "destructiveHint": false },
      "outputSchema": {
        "type": "object",
        "properties": {
          "status": { "type": "string", "description": "ok | error" },
          "post_urn": { "type": "string" },
          "count": { "type": "integer" },
          "comments": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "comment_urn": { "type": "string" },
                "author_name": { "type": "string" },
                "text": { "type": "string" },
                "created_at": { "type": "string" },
                "likes": { "type": "integer" }
              }
            }
          }
        },
        "required": ["status"]
      }
    },
    {
      "name": "search-posts",
      "description": "Search LinkedIn posts by keyword (stub — requires Content Search API or browser fallback)",
      "parameters": [
        {
          "name": "query",
          "type": "string",
          "description": "Search keywords",
          "required": true
        },
        {
          "name": "limit",
          "type": "integer",
          "description": "Maximum results (default 10, max 25)",
          "required": false
        }
      ],
      "annotations": { "readOnlyHint": true, "destructiveHint": false },
      "outputSchema": {
        "type": "object",
        "properties": {
          "status": { "type": "string", "description": "ok | error | stub" },
          "total_found": { "type": "integer" },
          "posts": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "post_urn": { "type": "string" },
                "author_name": { "type": "string" },
                "text": { "type": "string" },
                "created_at": { "type": "string" }
              }
            }
          }
        },
        "required": ["status"]
      }
    }
  ],
  "proxy": "scripts/linkedin_proxy.py"
}
