{
  "domain": "pharos.nexvigilant.com",
  "url_pattern": "/*",
  "title": "Pharos Reporting",
  "description": "Pharos intelligence reports, status monitoring, and analysis pipeline execution",
  "tools": [
    {
      "name": "pharos-report",
      "description": "Retrieve a specific or latest PHAROS surveillance report.",
      "parameters": [],
      "outputSchema": {
        "type": "object",
        "properties": {
          "status": {
            "type": "string",
            "description": "ok | error"
          }
        },
        "required": [
          "status"
        ]
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false
      }
    },
    {
      "name": "pharos-run",
      "description": "Run full L1-L5 validation",
      "parameters": [],
      "outputSchema": {
        "type": "object",
        "properties": {
          "status": {
            "type": "string",
            "description": "ok | error"
          }
        },
        "required": [
          "status"
        ]
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false
      }
    },
    {
      "name": "pharos-status",
      "description": "Check PHAROS status: existing reports, timer state.",
      "parameters": [],
      "outputSchema": {
        "type": "object",
        "properties": {
          "status": {
            "type": "string",
            "description": "ok | error"
          }
        },
        "required": [
          "status"
        ]
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false
      }
    }
  ],
  "proxy": "rust-native"
}
